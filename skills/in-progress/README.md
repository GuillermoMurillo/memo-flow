# in-progress

Skills under active development. **Not** listed in root `README.md` or `.claude-plugin/plugin.json`. Consumers who install via `/setup-memo-flow` will not see these.

Promote a skill out of here by moving it to a promoted bucket (`engineering/`, `productivity/`) and adding its path to `.claude-plugin/plugin.json` and the bucket's `README.md`.

- [memo-review](memo-review/SKILL.md): two-axis (Standards + Spec) review of a branch or PR via parallel sub-agents. Vendored from upstream `review`, renamed; behavior still evolving.
