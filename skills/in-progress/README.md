# in-progress

Skills under active development. **Not** listed in root `README.md` or `.claude-plugin/plugin.json`. Consumers who install via `/memo-flow` will not see these.

Promote a skill out of here by moving it to a promoted bucket (`engineering/`, `productivity/`) and adding its path to `.claude-plugin/plugin.json` and the bucket's `README.md`.

- [review-tests](review-tests/SKILL.md): test-sufficiency review of a diff — five-section report (M1–M4 Missing, C Consider) on whether existing tests cover the change. Add-on skill that composes with `/review` without forking it.
